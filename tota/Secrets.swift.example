// Copy this file to Secrets.swift and fill in your values.
// Secrets.swift is gitignored — never commit real credentials.
enum Secrets {
    static let supabaseURL = URL(string: "https://YOUR_PROJECT_REF.supabase.co")!
    static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"
}
